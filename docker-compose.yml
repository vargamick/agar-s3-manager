# Agar S3 Manager - Docker Compose
# Minimal S3 document management application
# Configured for Coolify deployment with Traefik

version: '3.8'

services:
  # API Server (Flask)
  api-server:
    build:
      context: ./api
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "3500"
    environment:
      - FLASK_ENV=${FLASK_ENV:-production}
      - API_PORT=3500
      - AWS_REGION=${AWS_REGION:-ap-southeast-2}
      - AWS_S3_ACCESS_KEY_ID=${AWS_S3_ACCESS_KEY_ID}
      - AWS_S3_SECRET_ACCESS_KEY=${AWS_S3_SECRET_ACCESS_KEY}
      - S3_BUCKET_NAME=${S3_BUCKET_NAME:-agar-documentation}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    volumes:
      - api-logs:/app/logs
    networks:
      - s3-manager-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3500/api/health/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Frontend (Nginx) - Main entry point with domain
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "80"
    depends_on:
      api-server:
        condition: service_healthy
    networks:
      - s3-manager-network
      - coolify
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    labels:
      - "traefik.enable=true"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
      - "traefik.http.middlewares.gzip.compress=true"
      - "traefik.http.routers.http-0-s3docs-frontend.entryPoints=http"
      - "traefik.http.routers.http-0-s3docs-frontend.middlewares=redirect-to-https"
      - "traefik.http.routers.http-0-s3docs-frontend.rule=Host(`s3docs.askagar.3dn.com.au`) && PathPrefix(`/`)"
      - "traefik.http.routers.http-0-s3docs-frontend.service=http-0-s3docs-frontend"
      - "traefik.http.routers.https-0-s3docs-frontend.entryPoints=https"
      - "traefik.http.routers.https-0-s3docs-frontend.middlewares=gzip"
      - "traefik.http.routers.https-0-s3docs-frontend.rule=Host(`s3docs.askagar.3dn.com.au`) && PathPrefix(`/`)"
      - "traefik.http.routers.https-0-s3docs-frontend.service=https-0-s3docs-frontend"
      - "traefik.http.routers.https-0-s3docs-frontend.tls=true"
      - "traefik.http.routers.https-0-s3docs-frontend.tls.certresolver=letsencrypt"
      - "traefik.http.services.http-0-s3docs-frontend.loadbalancer.server.port=80"
      - "traefik.http.services.https-0-s3docs-frontend.loadbalancer.server.port=80"

networks:
  s3-manager-network:
    driver: bridge
  coolify:
    external: true

volumes:
  api-logs:
