# Agar S3 Manager - Docker Compose
# Minimal S3 document management application
# Configured for Coolify deployment with Traefik

version: '3.8'

services:
  # API Server (Flask)
  api-server:
    build:
      context: ./api
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "3500"
    environment:
      - FLASK_ENV=${FLASK_ENV:-production}
      - API_PORT=3500
      - AWS_REGION=${AWS_REGION:-ap-southeast-2}
      - AWS_S3_ACCESS_KEY_ID=${AWS_S3_ACCESS_KEY_ID}
      - AWS_S3_SECRET_ACCESS_KEY=${AWS_S3_SECRET_ACCESS_KEY}
      - S3_BUCKET_NAME=${S3_BUCKET_NAME:-agar-documentation}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    volumes:
      - api-logs:/app/logs
    networks:
      - s3-manager-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3500/api/health/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Frontend (Nginx) - Main entry point with domain
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "80"
    depends_on:
      api-server:
        condition: service_healthy
    networks:
      - s3-manager-network
      - coolify
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.s3docs.rule=Host(`s3docs.askagar.3dn.com.au`)"
      - "traefik.http.routers.s3docs.entrypoints=https"
      - "traefik.http.routers.s3docs.tls=true"
      - "traefik.http.routers.s3docs.tls.certresolver=letsencrypt"
      - "traefik.http.services.s3docs.loadbalancer.server.port=80"
      - "traefik.docker.network=coolify"

networks:
  s3-manager-network:
    driver: bridge
  coolify:
    external: true

volumes:
  api-logs:
